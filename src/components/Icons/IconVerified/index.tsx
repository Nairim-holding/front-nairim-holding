import { IconProps } from "@/types/iconProps";
import { MdOutlineVerified } from "react-icons/md";

export default function IconVerified({size=20, color="#fff"}: IconProps){
    return <MdOutlineVerified size={size} color={color} className="svg-darkmode" />;
}