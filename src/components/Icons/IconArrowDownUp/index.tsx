import { IconProps } from "@/types/iconProps";
import { LuArrowDownUp } from "react-icons/lu";

export default function IconArrowDownUp({size=20, color="#fff"}: IconProps){
    return <LuArrowDownUp size={size} color={color} className="svg-darkmode" />;
}